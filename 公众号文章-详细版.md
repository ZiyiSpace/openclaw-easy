# OpenClaw 完整使用指南：从零到上手

> **三行命令配置你的 AI 助手，内置免费 API Key，开箱即用**

---

## 📖 目录

1. [什么是 OpenClaw](#什么是-openclaw)
2. [快速上手](#快速上手)
3. [完整命令列表](#完整命令列表)
4. [高级配置](#高级配置)
5. [常见问题](#常见问题)
6. [卸载与重装](#卸载与重装)

---

## 什么是 OpenClaw？

OpenClaw 是一个**自托管 AI 助手网关**，可以连接你常用的聊天应用到强大的 AI 能力。

### 核心功能

| 功能 | 说明 |
|------|------|
| 💬 **多端对话** | 网页控制面板、终端界面、命令行 |
| 🔧 **AI 编程** | 代码生成、调试、解释、重构 |
| 🔍 **工具调用** | 搜索网络、操作文件、执行命令 |
| 📱 **聊天接入** | WhatsApp、Telegram、Discord、iMessage |
| 🧠 **上下文记忆** | 记住对话历史，持续交流 |
| 🔒 **本地运行** | 数据在你机器上，安全可控 |

---

## 快速上手

### 第一步：安装

```bash
npm install -g @zi.yi/openclaw-easy
```

### 第二步：运行

```bash
openclaw-easy
```

你会看到：

```
✓ 使用内置免费 API key

🦞 OpenClaw 2026.2.9 — Think different. Actually think.

Updated ~/.openclaw/openclaw.json
Workspace OK: ~/.openclaw/workspace
Installed LaunchAgent
...
✓ 配置完成！Gateway 正在自动启动...
```

### 第三步：开始对话

```bash
npx openclaw dashboard
```

浏览器会自动打开控制面板，直接在网页里发消息即可！

---

## 完整命令列表

### 对话相关

| 命令 | 功能 |
|------|------|
| `npx openclaw dashboard` | 打开 Web 控制面板（推荐） |
| `npx openclaw tui` | 终端界面对话 |
| `npx openclaw agent --message "xxx"` | 直接发送单条消息 |

### 状态与日志

| 命令 | 功能 |
|------|------|
| `npx openclaw status` | 查看运行状态 |
| `npx openclaw logs` | 查看日志 |
| `npx openclaw sessions` | 查看会话历史 |

### 服务管理

| 命令 | 功能 |
|------|------|
| `npx openclaw gateway --restart` | 重启服务 |
| `npx openclaw gateway --stop` | 停止服务 |

---

## 高级配置

### 使用自己的 API Key

如果你有 OpenAI、Anthropic 等平台的 API Key：

```bash
# 方式一：命令行参数
openclaw-easy --openai-key sk-你的key

# 方式二：环境变量
export OPENAI_API_KEY=sk-你的key
openclaw-easy
```

系统会提示选择：
```
检测到你提供了自己的 API key。
  [1] 使用你自己的 API key
  [2] 使用内置的免费 API key

请选择 [1/2]（默认 1）：
```

### 更换模型

编辑配置文件：

```bash
nano ~/.openclaw/.env
```

修改 `OPENCLAW_MODEL` 变量：

```env
# 使用 GPT-4
OPENCLAW_MODEL=openai/gpt-4o

# 使用 Claude
OPENCLAW_MODEL=anthropic/claude-3-5-sonnet

# 使用其他 OpenRouter 模型
OPENCLAW_MODEL=openrouter/deepseek-chat
```

重启服务生效：

```bash
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl load ~/Library/LaunchAgents/ai.openclaw.gateway.plist 2>/dev/null
```

### 配置文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| 主配置 | `~/.openclaw/openclaw.json` | 模型、通道、会话设置 |
| 环境变量 | `~/.openclaw/.env` | API Key、模型名称 |
| 日志 | `~/.openclaw/logs/` | 运行日志 |

---

## 常见问题

### Q: 发消息没反应？

**检查清单：**

1. **确认 URL 正确** - 浏览器地址栏应包含 token（如 `#token=xxx`）
2. **检查服务状态** - 运行 `npx openclaw status`
3. **查看日志** - 运行 `npx openclaw logs`

### Q: 如何重启服务？

```bash
# macOS 手动重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl load ~/Library/LaunchAgents/ai.openclaw.gateway.plist 2>/dev/null

# 或使用 openclaw 命令
npx openclaw gateway --restart
```

### Q: 内置的是什么模型？

OpenRouter Trinity 免费模型，适合日常使用。如需更高性能，建议使用自己的 API Key。

### Q: 支持哪些平台？

- **操作系统**：macOS、Linux、Windows (WSL)
- **Node.js**：需要 18.0.0 或更高版本
- **聊天应用**：WhatsApp、Telegram、Discord、iMessage 等

### Q: 可以接入聊天应用吗？

可以，但需要在配置文件中设置。具体参考 OpenClaw 官方文档：https://docs.openclaw.ai

---

## 卸载与重装

### 完全卸载

重新安装前，建议先完全清理：

```bash
# 停止服务
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
rm -f ~/Library/LaunchAgents/com.openclaw.gateway.plist

# 清理配置
rm -rf ~/.openclaw

# 卸载 npm 包
npm uninstall -g @zi.yi/openclaw-easy
```

### 仅卸载 OpenClaw（保留 npm 包）

```bash
npx openclaw uninstall
```

---

## 项目信息

| 项目 | 信息 |
|------|------|
| **包名** | `@zi.yi/openclaw-easy` |
| **作者** | 王子怡 (Ziyi Wang) |
| **GitHub** | [ZiyiSpace/openclaw-easy](https://github.com/ZiyiSpace/openclaw-easy) |
| **OpenClaw** | [anthropics/openclaw](https://github.com/anthropics/openclaw) |
| **官方文档** | [docs.openclaw.ai](https://docs.openclaw.ai) |

---

## 技术说明

### 工作原理

```
1. 安装 npm 包
   ↓
2. 运行 openclaw-easy
   ↓
3. 读取内置配置（已加密的 API Key）
   ↓
4. 写入 ~/.openclaw/.env
   ↓
5. 调用 OpenClaw onboarding
   ↓
6. 安装并启动守护进程
   ↓
7. 打开控制面板，开始使用
```

### 内置配置

- **模型**：OpenRouter Trinity（免费）
- **Provider**：OpenRouter
- **加密方式**：XOR + base64
- **存储位置**：打包在 npm 包内

---

**开始你的 AI 助手之旅吧！** 🦞

---

*觉得有用的话，点个 ⭐ 支持一下*
